# Investment Casdoor 自助注册策略

状态：生效中
最后更新：2026-08-09

Investment 使用一个统一 FastAPI Backend 和两个独立 Casdoor OIDC Client：

| 表面 | Casdoor Application | 回调 | 自助注册 |
| --- | --- | --- | --- |
| Admin | `ADMIN_CASDOOR_APPLICATION` | `/api/auth/admin/callback` | 关闭 |
| Web | `WEB_CASDOOR_APPLICATION` | `/api/auth/web/callback` | 可开启 |

两个 Next.js 前端都通过本站同源 `/api` 访问同一个 `investment-backend`；不是由第二个 Nest/Web Backend 承担认证。Backend 使用独立 Client ID/Secret、redirect URI、cookie 名、session namespace、scope 与 Origin policy 隔离 Admin 和 Web。

## Web 注册流程

1. Web 登录页访问 `GET /api/auth/web/signup`；
2. Backend 创建一次性 OIDC 事务并重定向到 Casdoor signup authorization endpoint；
3. Casdoor 完成注册后仍走授权码 + PKCE 回调；
4. Backend 校验 state、nonce、issuer、audience 与签名，建立仅 Web 可用的服务端会话；
5. 首次登录按现有影子用户同步逻辑处理，不新增旁路注册协议。

Admin 只提供 `GET /api/auth/admin/login`，不得增加自助注册链接。Casdoor 侧必须关闭 Admin Application 的匿名注册，新 Web 用户默认角色不得包含任何管理权限。

## 运维门禁

- 两个 redirect URI 必须与严格 TLS 的公开地址逐字符一致；
- Client Secret 只通过 Secret 管理系统注入，不进入 Git、镜像、前端 bundle 或日志；
- 浏览器不得把 access token 写入 `localStorage`；
- 必须分别验证 Admin 登录、Web 登录、Web 注册、跨表面 cookie 隔离和退出登录；
- 认证契约以 `investment-backend/app/.env.example` 和实际路由为准。
